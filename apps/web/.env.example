# Supabase 프로젝트 설정(2a) — 값 채운 뒤 .env.local 로 복사해서 쓴다.
# Supabase 대시보드 > Project Settings > API Keys 에서 확인.
# 주의: 여기(그리고 .env.local 아닌 곳)엔 절대 secret key를 넣지 않는다 — 브라우저에 노출되면
# RLS를 우회할 수 있는 키라 유출과 같다. publishable(anon) key만 쓴다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
