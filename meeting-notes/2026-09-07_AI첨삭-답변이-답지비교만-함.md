# 2026-09-07 17:34 회의록 — AI 첨삭 답변이 답지 비교만 함

## 논의 주제
- "질문하기"에서 물음표만 입력해 첨삭을 받으면, "정답과 다르므로 틀림", "메모가 recalled를 명시하므로 recalled가 목표" 같은 동어반복 답이 나옴. 학습자는 왜 틀렸는지 알 수가 없음.
- (부수 이슈) 단어장 수정 폼 줄바꿈 안 됨 / 학습 화면에서 "알았어·몰랐어" 버튼이 안 보임.

## 진단 / 발견사항
- 첨삭 프롬프트가 정답 문장을 유일 기준으로 놓고 "단어 단위로 비교"하라고 지시 → 모델이 학습자 문장의 문법·의미를 독립 평가하지 않고, 글자가 다르면 오답 처리. 청크·문법 메모를 채점 기준처럼 인용.
- 단어장 수정 폼은 3칸이 모두 `<input>`(한 줄)이라 Enter가 폼 제출로 먹힘 → 줄바꿈 불가. 학습 카드 쪽은 이미 `<textarea>`. (이 건은 이후 커밋 7a00edb/50b28cb에 textarea 전환이 반영됨)
- "알았어·몰랐어" 버튼 안 보임: 코드는 정상(해당 phase에서 항상 렌더). 원인은 오래 열어둔 탭이 그 사이 재배포된 옛 번들을 물고 있어 일부 서브트리 렌더 실패. 시크릿 모드·브라우저 재시작으로 해결. 코드 변경 없음.

## 결정 사항
- 첨삭 프롬프트를 "먼저 학습자 문장을 독립 평가 → 차이를 (a)진짜 오류 (b)맞지만 표현만 다름 으로 분류 → 고친 문장" 구조로 변경.
- "정답과 다르다/답지와 달라서 틀리다"를 오답 근거로 대는 것 금지. 틀렸다면 반드시 문법 규칙·어형·뜻·자연스러움을 근거로.
- 청크·문법 메모는 참고 자료이지 채점 기준이 아님을 프롬프트에 명시.
- 일반 질문(물음표 아님) 경로에도 같은 원칙 한 줄 추가.

## 변경 내역
- `/Users/a405581/projects/공부의비결_leitner-voca-app/supabase/functions/ask-teacher-proxy/index.ts` — `buildPrompt()` 첨삭 지시 재작성(독립 평가 → 차이 분류 → 수정문, "답지와 달라서" 금지, 메모는 참고용 명시)
- 실행(상태 변경): `supabase functions deploy ask-teacher-proxy` (배포 완료, 프로덕션 반영됨)
- 참고: 이번 세션 초반의 "인사치례 제거"는 별도 회의록(2026-09-07_AI질문-답변-인사치례-제거.md)에 기록됨.

## 다음 액션
- 앱에서 실제 오답 케이스로 물음표 첨삭을 다시 돌려, 왜 틀렸는지 언어적 근거가 나오는지 확인.
- 오래 열어둔 탭 캐시 문제 재발 시 ⌘⇧R → 안 되면 브라우저 재시작.

## 세션 참조
- Claude Code 세션 ID: `30e8efa6-eb1f-4b24-9cb9-fef0cef8cec4`
- 로컬 트랜스크립트: `/Users/a405581/.claude/projects/-Users-a405581-projects-------leitner-voca-app/30e8efa6-eb1f-4b24-9cb9-fef0cef8cec4.jsonl`
- 이어서 열기: `claude --resume 30e8efa6-eb1f-4b24-9cb9-fef0cef8cec4`
